FROM python:3.11-slim

# Set environment variables
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# Set work directory
WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    postgresql-client \
    gcc \
    python3-dev \
    musl-dev \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*

# Install Python dependencies (dev.txt pulls in base.txt too; this image is used by
# docker-compose for local dev, where DJANGO_SETTINGS_MODULE defaults to config.settings.dev
# and requires django-debug-toolbar etc.)
COPY requirements/ requirements/
RUN pip install --upgrade pip && \
    pip install -r requirements/dev.txt

# Copy project
COPY . .

# Create directories
RUN mkdir -p staticfiles media logs

# Make entrypoint executable
RUN chmod +x /app/entrypoint.sh || true

EXPOSE 8000

CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
